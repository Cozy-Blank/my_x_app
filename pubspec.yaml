name: my_x_app
description: "X text extractor app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '^3.12.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  html: ^0.15.4
  shared_preferences: ^2.5.5

flutter:
  uses-material-design: true